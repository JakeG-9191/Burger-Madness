<h1>Fricken Burger Madness!</h1>

<h2>These one's have already been devoured...</h2>

<ul class="not_eaten">
  {{#each burgers}}
    {{#unless devoured}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These one's are available for consumption...</h2>

<ul class="eaten">
  {{#each burgers}}
    {{#if devoured}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to the Madness</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgs">Burger Name:</label>
    <input type="text" id="burgs" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>