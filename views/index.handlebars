<h1>Welcome to the Burger Madness!</h1>

<h2>These one's have already been devoured...but they can be reclaimed!</h2>

<ul class="not_eaten">
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These one's are available for consumption...hurry and choose before they are all gone!</h2>

<ul class="eaten">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to the Madness</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgs">Burger Name:</label>
    <input type="text" id="burgs" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>